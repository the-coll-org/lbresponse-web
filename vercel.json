{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://crn-api.thecoll.org/api/:path*"
    },
    {
      "source": "/health/:path*",
      "destination": "https://crn-api.thecoll.org/health/:path*"
    }
  ]
}
