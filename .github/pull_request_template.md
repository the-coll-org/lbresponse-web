## Summary

## Changes

## How to Test

1.
2.

## Checklist

- [ ] Code passes lint (`npm run lint`)
- [ ] Code is formatted (`npm run format:check`)
- [ ] TypeScript compiles (`npm run type-check`)
- [ ] Build succeeds (`npm run build`)
- [ ] Translations updated for both EN and AR (if UI changes)
- [ ] Tested in both LTR and RTL layouts (if UI changes)
- [ ] Tested in both light and dark themes (if UI changes)
